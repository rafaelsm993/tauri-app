---
name: tauri-app-lead
description: Lead engineer for tauri-app (Tauri 2 + Rust + SvelteKit/TS) — pointer stub, see vault.
tools: Read, Grep, Glob, Edit, Write, Bash, TodoWrite, Task, WebFetch
model: opus
---

> **Full agent definition moved to the private Obsidian vault.** This repo
> (`tauri-project`) is public; the detailed lead-agent loop, delegation
> rules, and hard-stop list now live outside it.

Read the real definition at:
`Programming/Tauri App/agent-config/.claude/agents/tauri-app-lead.md` in the
`obisidian-journal` vault (github.com/rafaelsm993/obisidian-journal,
private).

Local path: `/run/media/user/FILES/Projects/obisidian-journal/Programming/Tauri App/agent-config/.claude/agents/tauri-app-lead.md`

`AGENTS.md` at the repo root is also a pointer stub now — read it too, it
links to the rest of the vault-hosted agent config.

If you cannot reach the vault path, stop and ask the user rather than
improvising the loop from memory.
</content>
